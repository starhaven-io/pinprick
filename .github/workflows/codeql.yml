name: CodeQL

on:
  push:
    branches:
      - main
    paths:
      - "**/*.rs"
      - "**/Cargo.toml"
      - "**/Cargo.lock"
      - "**/rust-toolchain*"
      - "**/rust-project.json"
      - "**/.cargo/**"
      - "audited-actions/**"
      - "catalog-minisign.pub"
      - ".github/workflows/**"

concurrency:
  group: "codeql-${{ github.ref }}"
  cancel-in-progress: false

permissions: {}

jobs:
  analyze:
    name: Analyze
    permissions:
      contents: read
      security-events: write # required to upload SARIF results
    uses: starhaven-io/.github/.github/workflows/reusable-codeql.yml@a2ff1457a008d2fe895f044330469e38d6d97a6d # v2026.09.10.3
    with:
      languages: '["actions","rust"]'
      runner: "ubuntu-24.04"
      timeout-minutes: 30
      build-mode: "none"
      build-profile: ""
